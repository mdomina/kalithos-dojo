# Target: Flask/Jinja2 Server-Side Template Injection -> RCE. Vulhub (MIT).
# Immagine PINNATA a digest. Python (Jinja2). App demo in ./src/app.py. Train (SSTI).
name: kalithos-dojo-flask-ssti

services:
  web:                            # TARGET
    image: vulhub/flask@sha256:20d202d35fe99818878a3f844362210a21894bfab57b8acf23dfa3ade9a87026
    volumes:
      - ./src:/app
    networks: [lab]
    # NIENTE 'ports:' -> isolamento

  attacker:                       # ATTACCANTE — kali-lite (dipendenza harness)
    image: grpo-rt/kali-lite:latest
    networks: [lab]
    command: sleep infinity

networks:
  lab:
    internal: true                # zero egress
