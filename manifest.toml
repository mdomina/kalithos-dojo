# Indice del range — classe / stack / split. La metrica del progetto è il success-rate
# su HELD-OUT (generalizzazione), non su train (memorizzazione).
#
# split: "train" (usato per allenare) | "held-out" (CONGELATO, solo eval finale)
# Regola anti-leakage: ogni held-out ha la sua classe coperta altrove in train, ad app diversa.

[[target]]
id = "thinkphp-5-rce"
class = "command-injection"
stack = "php"
split = "train"

[[target]]
id = "drupal-cve-2018-7600"
class = "form-rce"
stack = "php"
split = "train"

# --- da migrare / aggiungere (seguendo ONBOARDING.md) ---
# tomcat-cve-2017-12615  class=upload-webshell stack=java split=train   (build-based: copiare build-context)
# struts2-s2-001         class=ognl            stack=java split=train
# weblogic-cve-2017-10271 class=deserialization stack=java split=train
# confluence-cve-2022-26134 class=ognl        stack=java split=held-out  (classe vista via struts)
# spring-cve-2022-22965  class=deserialization stack=java split=held-out (classe vista via weblogic)
